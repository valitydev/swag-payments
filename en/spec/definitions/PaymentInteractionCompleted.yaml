type: object
allOf:
  - 
    $ref: '#/definitions/InvoiceChange'
  - 
    type: object
    description: |
      Оповещение о завершении последнего запрошенного взаимодействия с плательщиком
    required:
      - paymentID
    properties:
      paymentID:
        description: Идентификатор платежа
        type: string
      userInteraction:
        $ref: '#/definitions/UserInteraction'
