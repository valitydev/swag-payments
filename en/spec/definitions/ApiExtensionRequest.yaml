type: object
allOf:
  - 
    $ref: "#/definitions/UserInteraction"
  - 
    type: object
    required:
      - apiType
    properties:
      apiType:
        description: API type to use in subsequent requests
        type: string
