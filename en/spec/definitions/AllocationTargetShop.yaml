description: Магазин как назначение транзакции
allOf:
  - 
    $ref: '#/definitions/AllocationTarget'
  - 
    type: object
    required:
      - shopID
    properties:
      shopID:
        description: Shop ID
        type: string
        maxLength: 40
        minLength: 1
