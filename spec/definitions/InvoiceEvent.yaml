type: object
required:
  - id
  - createdAt
  - changes
properties:
  id:
    type: integer
  createdAt:
    $ref: "#/definitions/CreatedAt"
  changes:
    type: array
    items:
      $ref: "#/definitions/InvoiceChange"
