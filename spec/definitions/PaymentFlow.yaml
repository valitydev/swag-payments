type: object
discriminator: type
required:
  - type
properties:
  type:
    description: Тип процесса выполнения платежа
    type: string
    enum:
      - PaymentFlowInstant
      - PaymentFlowHold
    default: PaymentFlowInstant
