get:
  description: Получить список провайдеров платёжных сервисов
  tags:
    - PaymentInstitutions
  operationId: getServiceProviders
  parameters:
    - $ref: '#/parameters/requestID'
    - $ref: '#/parameters/deadline'
  responses:
    '200':
      description: Список провайдеров платёжных сервисов
      schema:
        type: array
        items:
          $ref: '#/definitions/ServiceProvider'
    '400':
      $ref: '#/responses/DefaultLogicError'
